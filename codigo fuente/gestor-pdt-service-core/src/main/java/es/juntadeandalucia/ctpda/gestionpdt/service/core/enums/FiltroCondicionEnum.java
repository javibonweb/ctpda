package es.juntadeandalucia.ctpda.gestionpdt.service.core.enums;

/**
 * The enum Filtro condicion enum.
 */
public enum FiltroCondicionEnum {

	/** Mayor que. */
	GT,

	/** Menor que. */
	LT,

	/** Mayor o igual que. */
	GOE,

	/** Menor o igual que. */
	LOE,

	/** Igual que. */
	EQ,

	/** Contiene. */
	LIKE,
}
